{
  "name": "root",
  "private": true,
  "scripts": {
    "clean": "lerna run clean",
    "lint": "lerna run lint",
    "build": "lerna run build",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
