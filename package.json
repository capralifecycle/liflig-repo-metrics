{
  "name": "root",
  "private": true,
  "scripts": {
    "clean": "lerna run clean",
    "lint": "lerna run lint",
    "lint:fix": "lerna run lint:fix",
    "build": "lerna run build",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "5.5.4"
  },
  "resolutions": {
    "@types/webpack": "^5.18.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
